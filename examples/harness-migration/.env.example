# OpenAI is the default provider for all three sub-examples. Required.
OPENAI_API_KEY=sk-...

# Optional model override.
# LLM_MODEL=gpt-4o-mini

# Optional FastAIAgent Platform connection (--connect flag on the runners).
# FASTAIAGENT_API_KEY=fa_k_...
# FASTAIAGENT_PROJECT=harness-migration
