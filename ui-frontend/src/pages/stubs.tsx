/**
 * Reserved for future placeholder routes.
 *
 * Sprint 1's stubs (CompareTracesPage) have been replaced by real pages —
 * the file is kept so a future slice can drop a stub here without touching
 * the App.tsx import order.
 */

export {};
