"""Local web UI server and supporting modules.

This package ships the FastAPI backend for `fastaiagent ui`. The HTTP/WebSocket
layer and frontend static bundle live under here; storage primitives shared with
the rest of the SDK live in `fastaiagent.ui.db`.
"""
